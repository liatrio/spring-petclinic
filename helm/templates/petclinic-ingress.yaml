apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: petclinic-ingress-{{ .Values.environment }}
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      host: {{ .Values.environment }}.a910146643c8611e98dfe0a264e7ebfa-575602821.us-east-1.elb.amazonaws.com
      paths:
#      - path: /{{ .Values.environment }}/petclinic
      - path: /petclinic
        backend:
          serviceName: petclinic-service-{{ .Values.environment }}
          servicePort: 80
